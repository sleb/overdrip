{
  "name": "overdrip",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "devDependencies": {
    "@types/bun": "1.3.2"
  },
  "scripts": {
    "cli:dev": "cd packages/cli && bun src/cli.ts",
    "cli:build": "bun packages/cli/build.ts build",
    "cli:build:release": "bun packages/cli/build.ts build:release",
    "cli:clean": "bun packages/cli/build.ts clean"
  }
}
